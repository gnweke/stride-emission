// Loads the generated Anchor IDL from ../target/idl/stride_emission.json
// If you relocate this repo structure later, adjust the path or pass config.idl.
import idl from "../../target/idl/stride_emission.json" assert { type: "json" };
export default idl;
